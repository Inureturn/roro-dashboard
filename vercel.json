{
  "version": 2,
  "framework": "vite",
  "installCommand": "npm i -g pnpm@10.18.0 && cd web && pnpm install",
  "buildCommand": "npm i -g pnpm@10.18.0 && cd web && pnpm run build",
  "devCommand": "cd web && pnpm run dev",
  "outputDirectory": "web/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/" }
  ]
}
